import 'package:flutter/material.dart';

class AppColors {
  // Brand colors
  // static const Color primary = Color.fromARGB(255, 33, 148, 224);
  static const Color primary = Color(0xFF019ACD);
  static const Color primaryDark = Color(0xFF00A3CF);
  static const Color secondary = Color.fromARGB(255, 252, 135, 57);

  // Utility colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;
  static const Color redAccent = Colors.redAccent;

  // Modifiers
  static const Color tint = Color(0xFF27ADD5);
  static const Color highlight = Color(0xFF14A6D2);
  static const Color grey = Color.fromARGB(255, 162, 165, 167);
  static const Color outline = Color(0xFFE0E0E0);
  static const Color darkGrey = Color.fromARGB(255, 73, 74, 75);
}
